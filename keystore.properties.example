# Copy to keystore.properties (git-ignored) and fill in your real values.
# storeFile is relative to the app/ module (so ../ points at the repo root).
storeFile=../upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
